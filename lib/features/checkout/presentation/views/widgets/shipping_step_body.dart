import 'package:flutter/material.dart';

class ShippingStepBody extends StatelessWidget {
  const ShippingStepBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: []);
  }
}
